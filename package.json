{
  "name": "voicepilot",
  "version": "1.0.8",
  "description": "A conversational accessibility layer for the internet — browse websites completely through voice.",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-web-extension": "^4.3.0"
  },
  "dependencies": {
    "@elevenlabs/client": "^1.0.0"
  }
}
